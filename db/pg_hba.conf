# TYPE  DATABASE        USER            ADDRESS                 METHOD

# Local connections
local   all             all                                     trust
host    all             all             127.0.0.1/32            trust

# Replication connections from replicas
host    replication     news_user       0.0.0.0/0               md5

# Application connections
host    all             all             0.0.0.0/0               md5